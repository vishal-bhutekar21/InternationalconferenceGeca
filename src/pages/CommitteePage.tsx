import Committee from '../components/Committee';

const CommitteePage = () => {
    return (
        <main style={{ paddingTop: '80px', backgroundColor: 'var(--color-bg-soft-slate)', minHeight: '100vh' }}>
            <Committee />
        </main>
    );
};

export default CommitteePage;
